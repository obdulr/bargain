services:
  - type: web
    name: bargain-web
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: cd bargain-web && npm start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://bargainhuntrs.com
      - key: NODE_VERSION
        value: 24
